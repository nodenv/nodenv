../libexec/nodenv-realpath.dylib:
	@echo "Warning: this Makefile is obsolete and kept only for backwards compatibility." >&2
	@echo "You can remove the \`configure && make ...' step from your nodenv setup." >&2

clean:
	rm -f *.o ../libexec/*.dylib

.PHONY: ../libexec/nodenv-realpath.dylib clean
